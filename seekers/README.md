Seeker profiles live in this directory, one Markdown file per profile.

Filename format: `name-slug.md` (e.g. `jane-doe.md`).

Each file has YAML frontmatter plus a Markdown body for the detailed “about” text.

